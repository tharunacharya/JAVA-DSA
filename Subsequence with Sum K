class Solution {
    public boolean checkSubsequenceSum(int[] arr, int k) {
        // code here
        int n=arr.length;
        int dp[][]=new int[n][k+1];
        
        //base case
        for(int i=0;i<n;i++){
            dp[i][0]=1;
        }
        
        for(int t=1;t<=k;t++){
            if(t==arr[0]){
                dp[0][t]=1;
            }else{
            dp[0][t]=2;
            }
        }
        
        for(int i=1;i<n;i++){
            for(int j=1;j<=k;j++){
                int pick =2;
                if(arr[i]<=j){
                    pick=dp[i-1][j-arr[i]];
                    if(pick==1){
                        dp[i][j]=1;
                        continue; 
                    }
                }
                int noPick=dp[i-1][j];
                dp[i][j]=noPick;
            }
        }
        return (dp[n-1][k]==1);
    }
    //0->false
    //1-true;
    //2->seen and true
    
    // public boolean rec(int[] arr, int k, int i,int dp[][]){
    //     //base case
    //     if(k==0){
    //         dp[i][k]=1;
    //         return true;
    //     }
        
    //     //second base case
    //     if(i==0){
    //         if(arr[i]==k) {
    //             dp[i][k]=1;
    //             return true;
    //         }
    //         dp[i][k]=2;
    //         return false;
    //     }
        
    //     if(dp[i][k]!=0){
    //         return (dp[i][k]==1?true:false);
    //     }
    //     boolean pick=false;
    //     if(arr[i]<=k){
    //         pick=rec(arr,k-arr[i],i-1,dp);
    //         if(pick){
    //             dp[i][k]=1;
    //             return true;
    //         }
    //     }
    //     boolean noPick=rec(arr,k,i-1,dp);
    //     dp[i][k]=(noPick==true)?1:2;
        
    //     return noPick;
    // }
}
